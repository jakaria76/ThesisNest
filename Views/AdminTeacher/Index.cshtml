@model IEnumerable<ThesisNest.Models.TeacherProfile>
@{
    ViewData["Title"] = "Teacher Management";
}

<div class="container-fluid py-4">
    <!-- Header -->
    <div class="d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between mb-4 gap-3">
        <h2 class="mb-0">Teacher Management</h2>
        <a asp-action="Create" class="btn btn-primary d-flex align-items-center gap-2">
            <i class="bi bi-plus-lg"></i> Add Teacher
        </a>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-warning shadow-sm rounded">No teachers found.</div>
    }
    else
    {
        <div class="table-responsive shadow-sm rounded">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-dark text-uppercase small">
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Phone</th>
                        <th>Dept</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var t in Model)
                    {
                        <tr class="align-middle">
                            <td>@t.Id</td>
                            <td>
                                <div class="d-flex align-items-center gap-2">
                                    <img src="@Url.Action("Photo", "TeacherProfile", new { id = t.Id })"
                                         alt="Profile" class="rounded-circle" width="40" height="40" />
                                    <span>@t.FullName</span>
                                </div>
                            </td>
                            <td>@t.Email</td>
                            <td>@t.Phone</td>
                            <td>@t.Department</td>
                            <td class="text-end">
                                <div class="d-flex flex-column flex-sm-row justify-content-end gap-2">
                                    <a asp-action="Edit" asp-route-id="@t.Id" class="btn btn-sm btn-outline-primary d-flex align-items-center gap-1">
                                        <i class="bi bi-pencil"></i> Edit
                                    </a>

                                    <form asp-action="Delete" asp-route-id="@t.Id" method="post" class="d-inline"
                                          onsubmit="return confirm('Delete TEACHER PROFILE (not user)?');">
                                        @Html.AntiForgeryToken()
                                        <button class="btn btn-sm btn-outline-danger d-flex align-items-center gap-1">
                                            <i class="bi bi-trash"></i> Delete
                                        </button>
                                    </form>

                                    <form asp-action="DeleteAccount" asp-route-id="@t.Id" method="post" class="d-inline"
                                          onsubmit="return confirm('Delete TEACHER USER ACCOUNT? This is permanent.');">
                                        @Html.AntiForgeryToken()
                                        <button class="btn btn-sm btn-danger d-flex align-items-center gap-1">
                                            <i class="bi bi-person-x"></i> Delete Account
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>

<style>
    /* Mobile adjustments */
    @@media (max-width: 575.98px) {
        .table tbody td

    {
        font-size: 0.85rem;
    }

    .table td img {
        width: 30px;
        height: 30px;
    }

    .d-flex.flex-column.flex-sm-row {
        flex-direction: column !important;
        gap: 0.25rem;
    }

    .btn-sm i {
        font-size: 0.85rem;
    }

    }

    /* Optional: table row hover shadow */
    .table-hover tbody tr:hover {
        background-color: #f8f9fa;
    }
</style>
