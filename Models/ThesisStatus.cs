namespace ThesisNest.Models
{
    public enum ThesisStatus
    {
        Pending,    // Student uploaded, pending teacher review
        Accept,   // Teacher approved
        Rejected     // Teacher rejected
    }
}
